import { memo } from 'react';

export const NavigationHeader = memo(() => (
  <h4 className="text-sm font-medium text-gray-400 mb-4">Spis treści</h4>
));

NavigationHeader.displayName = 'NavigationHeader';
